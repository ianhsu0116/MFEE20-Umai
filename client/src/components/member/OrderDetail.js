import React from "react";

const OrderDetail = (props) => {
  return (
    <div className="OrderDetail">
      <div className="OrderDetail-container"></div>
    </div>
  );
};

export default OrderDetail;
